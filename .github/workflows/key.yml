name: Secret Key
run-name: ${{github.actor}} requesting access token
on: [push]

jobs:
  retrieving-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Getting Key
      - shell: bash
        env:
          super_secret: ${{ secrets.SuperSecret }}
        run: |
          retrv "$super_secret"
